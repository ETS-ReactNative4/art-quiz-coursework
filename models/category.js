class Category {
  constructor(id, title, color, greyImage, colorImage) {
    this.id = id;
    this.title = title;
		this.color = color;
    this.greyImage = greyImage;
    this.colorImage = colorImage;
  }
}

export default Category;
